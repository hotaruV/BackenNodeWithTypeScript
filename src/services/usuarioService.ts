import { Models } from '../models/models'; // Importa la instancia de modelos

class UserService {
    // Método para obtener todos los usuarios
    async getAllUsers() {
        
    }
}

export default new UserService();
